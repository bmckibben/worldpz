<%= form_for(@recipe_category) do |f| %>
  <% if @recipe_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recipe_category.errors.count, "error") %> prohibited this recipe_category from being saved:</h2>

      <ul>
      <% @recipe_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">
  <div class="col-md-4 col-xs-12 field">
    <%= f.label :category_name %><br>
    <%= f.text_field :category_name %>
  </div>
</div>

<div class="row">
 <div class="col-md-12" >
      <%= link_to 'Show', @recipe_category,  class: "btn btn-default  pull-right" %>
    <%= link_to 'Back', recipe_categories_path,  class: "btn btn-default  pull-right" %> 
    <%= f.submit class: "btn btn-primary pull-right" %>
  </div>
</div>  
<% end %>
