<%= form_for(@blog_comment) do |f| %>
  <% if @blog_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog_comment.errors.count, "error") %> prohibited this blog_comment from being saved:</h2>

      <ul>
      <% @blog_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :commentable_id %><br>
    <%= f.number_field :commentable_id %>
  </div>
  <div class="field">
    <%= f.label :commentable_type %><br>
    <%= f.text_field :commentable_type %>
  </div>
  <div class="field">
    <%= f.label :publish %><br>
    <div class="switch" >
    <%= f.check_box :publish, {id:"cmn-toggle-1",  class:"cmn-toggle cmn-toggle-round"}, checked_value="1", unchecked_value="0"  %>
    <label for="cmn-toggle-1"></label>
    </div>
  </div>  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
